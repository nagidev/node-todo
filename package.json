{
  "name": "todo",
  "version": "1.0.0",
  "description": "A simple todo app using basic nodejs ib the backend and react with redux in the frontend.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js --ignore 'data.json'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
